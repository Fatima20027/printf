#ifndef HEADER_FILE
#define HEADER_FILE

#include <stdio.h>
#include <unistd.h>

void _putchar(char c);
int _printf(const char *format, ...);
int allspechandler(char specifier, va_list hoho);

#endif

